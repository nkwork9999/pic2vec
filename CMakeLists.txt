cmake_minimum_required(VERSION 3.5...3.29)

set(TARGET_NAME eupe)
set(EXTENSION_NAME ${TARGET_NAME}_extension)
set(LOADABLE_EXTENSION_NAME ${TARGET_NAME}_loadable_extension)

project(${TARGET_NAME})

set(CMAKE_POSITION_INDEPENDENT_CODE ON)

# ONNX Runtime
find_package(onnxruntime CONFIG REQUIRED)

set(EXTENSION_SOURCES
    src/eupe_extension.cpp
    src/onnx_inference.cpp
    src/image_utils.cpp
    src/embedded/tiny_model_data.cpp
)

include_directories(src/include)

build_static_extension(${TARGET_NAME} ${EXTENSION_SOURCES})
build_loadable_extension(${TARGET_NAME} " " ${EXTENSION_SOURCES})

target_link_libraries(${EXTENSION_NAME} onnxruntime::onnxruntime)
target_link_libraries(${LOADABLE_EXTENSION_NAME} onnxruntime::onnxruntime)

install(
  TARGETS ${EXTENSION_NAME}
  EXPORT "${DUCKDB_EXPORT_SET}"
  LIBRARY DESTINATION "${INSTALL_LIB_DIR}"
  ARCHIVE DESTINATION "${INSTALL_LIB_DIR}")
