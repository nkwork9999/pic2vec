build/
.cache/
*.o
*.so
*.dylib
*.dll
*.duckdb_extension
.DS_Store

# Rust build artifacts
rust/target/
**/target/

# CMake/CCache
.cache/
CMakeFiles/
CMakeCache.txt

# Legacy: old script versions wrote the embedded-model .cpp here. The current
# build writes to ${CMAKE_BINARY_DIR}/generated/ instead, but stale ~131MB
# leftovers in src/embedded/ from previous local runs must never be committed.
src/embedded/

